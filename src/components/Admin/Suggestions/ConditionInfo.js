import React, { useState, useEffect } from "react";
import { Typography } from "@material-ui/core";

const ConditionInfo = (props) => {
  return <Typography>Conditions:</Typography>;
};

export default ConditionInfo;
